# Place holder for kernel .config
# Generate one via:
#   cd build/kernel/linux-6.6.54 && make defconfig
# Then adjust for ftlinux requirements (ext4, efi, dhcp, etc.)
# Copy the resulting .config into this file when ready.
