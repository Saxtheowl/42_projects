# Roadmap post-review

- Integrate `C/ft_helpme/scripts/reports/rmse_plot.py` into the repo or pin a vendor copy to avoid cross-project dependency drift.
- Add a small CLI wrapper around the RMSE helper to expose `--png` and summary output as a first-class command.
- Extend validation history exports to include bootstrap averages for long-term trend reporting.
