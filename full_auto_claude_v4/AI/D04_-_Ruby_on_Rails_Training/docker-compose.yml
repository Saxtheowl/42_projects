version: '3.8'

services:
  rails:
    image: ruby:3.2
    volumes:
      - .:/app
    working_dir: /app
    command: bash -c "gem install rails -v '7.1.5' bundler && tail -f /dev/null"
    ports:
      - "3000:3000"
