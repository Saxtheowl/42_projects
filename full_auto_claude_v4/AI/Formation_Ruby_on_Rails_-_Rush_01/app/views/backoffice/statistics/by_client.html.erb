<h1>Statistics by Client</h1>

<div class="card">
  <%= form_tag backoffice_statistics_by_client_path, method: :get, class: 'form-inline' do %>
    <div class="form-group">
      <%= select_tag :client_id, options_from_collection_for_select(@clients, :id, :full_name, params[:client_id]), { include_blank: 'Select Client', class: 'form-control' } %>
    </div>
    <div class="form-group">
      <label>From:</label>
      <%= text_field_tag :start_date, @start_date, class: 'form-control datepicker' %>
    </div>
    <div class="form-group">
      <label>To:</label>
      <%= text_field_tag :end_date, @end_date, class: 'form-control datepicker' %>
    </div>
    <%= submit_tag 'Apply', class: 'btn btn-primary' %>
  <% end %>
</div>

<% if @client %>
  <div class="row">
    <div class="col-md-6">
      <div class="card">
        <h3>Profit for <%= @client.full_name %></h3>
        <div id="profit-chart"></div>
      </div>
    </div>
    <div class="col-md-6">
      <div class="card">
        <h3>Orders for <%= @client.full_name %></h3>
        <div id="orders-chart"></div>
      </div>
    </div>
  </div>

  <script src="https://code.highcharts.com/highcharts.js"></script>
  <script>
    Highcharts.chart('profit-chart', {
      title: { text: 'Weekly Profit' },
      xAxis: { categories: <%= raw @profit_data[:weeks].to_json %> },
      yAxis: { title: { text: 'EUR' } },
      series: [{ name: 'Profit', data: <%= raw @profit_data[:profit].to_json %> }]
    });

    Highcharts.chart('orders-chart', {
      title: { text: 'Weekly Orders' },
      xAxis: { categories: <%= raw @orders_data[:weeks].to_json %> },
      yAxis: { title: { text: 'EUR' } },
      series: [{ name: 'Orders', data: <%= raw @orders_data[:orders].to_json %> }]
    });
  </script>
<% else %>
  <div class="alert alert-info">Please select a client to view statistics.</div>
<% end %>
